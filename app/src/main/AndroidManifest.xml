<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.Light"
        tools:targetApi="31">
        <activity
            android:name=".ui.piece.ranobeID.RanobeIDActivity"
            android:exported="false" />
        <activity
            android:name=".ui.piece.mangaID.MangaIDActivity"
            android:exported="false" />
        <activity
            android:name=".ui.piece.animeID.AnimeIDActivity"
            android:exported="false" />
        <activity
            android:name=".AppShellActivity"
            android:exported="false"
            android:label="@string/title_activity_app_shell" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>